# 💳 React Pricing Cards

A responsive pricing card UI built with React, showcasing dynamic data rendering,
component reusability, and clean modern design.

## 🚀 Features

- Dynamic data rendering using `.map()`
- Reusable `PricingCard` component
- Conditional rendering for available/unavailable features
- Responsive design 
- Smooth animations
- Clean gradient background



